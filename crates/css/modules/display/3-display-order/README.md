# Chapter 3 — Display Order (order)

Implements CSS Display Level 3 §3 “Display Order: the order property”.

- Spec: https://www.w3.org/TR/css-display-3/#order-property
- Mapping: see `../spec.md` One-to-one mapping.

Note: Source files placed here should include inline comments referencing the exact spec clause (e.g., `§3`, `§3.1`).
