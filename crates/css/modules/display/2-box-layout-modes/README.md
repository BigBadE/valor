# Chapter 2 — Box Layout Modes

This folder aggregates code and docs that implement CSS Display Level 3 §2 “Box Layout Modes: the display property”.

- Spec: https://www.w3.org/TR/css-display-3/#the-display-properties
- Mapping: see `../spec.md` One-to-one mapping.

Note: Source files placed here should include inline comments referencing the exact spec clause (e.g., `§2.1`, `§2.3`).
